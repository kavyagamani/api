package cucumber;

import io.cucumber.junit.platform.engine.Cucumber;

@Cucumber
public class RunCucumberTest {
    // This class is intentionally left blank. The @Cucumber annotation
    // enables the Cucumber JUnit Platform Engine to discover and run
    // feature files under src/test/resources.
}
